import { type View } from "@/types"

export async function getViews(entity: string): Promise<View[]> {
    // For now, return an empty array. In the future, this could fetch views from an API or database
    return []
} 